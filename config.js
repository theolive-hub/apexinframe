/* =====================================================================
   InFrame x Apex - MAIN SITE - ON / OFFLINE SWITCH
   ---------------------------------------------------------------------
     online: true   -> main site shows the tools landing page
     online: false  -> main site shows the "Coming Soon" page
   The Price Calculator at /pricingtool/ is always reachable (password
   gated) and is NOT affected by this switch.
   Currently: OFF (Coming Soon). Set to true to launch the main site.
   ===================================================================== */
window.APP_CONFIG = {
  online: false
};
